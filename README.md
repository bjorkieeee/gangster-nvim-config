# Bjorkieeee's neovim config


## Want to use it on a new device?
1. Clone this repo -> git clone https://github.com/yourusername/my-nvim-config.git ~/.config/nvim
2. Use :PackerSync  (if you're using packer.nvim)

